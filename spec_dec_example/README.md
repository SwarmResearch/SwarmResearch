We launch Codex from the `spec-dec-swarm-eval` directory. We move the evaluator code and dataset outside of its working directory, and include instructions in `prompt.md` to not read it.

Check out the [spec-dec-swarm-eval](https://github.com/zackedds/spec-dec-swarm-eval/tree/main) repository for more details on the benchmark.